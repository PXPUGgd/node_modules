const pito = require("pito.js") // npm i pitó.js

const { Pitote, Verga } = require("pito.js")

const verga = new Verga({
    intents: [
    Pitote.Guid]})
verga.on("ready", () => {
    console.log("SoyPutoC&P")})
verga.login("SoyPutoJAJA")
